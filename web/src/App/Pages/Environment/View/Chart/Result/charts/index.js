import barByDate from './barByDate'
import lineByDate from './lineByDate'
import barByValue from './barByValue'
import lineByValue from './lineByValue'
import barGroupedByValues from './barGroupedByValues'

export default {
  barByDate,
  barByValue,
  lineByDate,
  lineByValue,
  barGroupedByValues
}
