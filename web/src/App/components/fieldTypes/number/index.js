import NumberField from 'orionsoft-parts/lib/components/fields/numeral/Number'
import View from './View'

export default {
  field: NumberField,
  view: View
  // no passed view, just render the value
}
